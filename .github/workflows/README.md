# PackMaster CI

`apply-smart-matcher.yml` is the repository regression-test workflow. The filename is retained to avoid unnecessary workflow churn, but the workflow now only runs tests and does not modify source files.
