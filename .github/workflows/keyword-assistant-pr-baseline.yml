name: Keyword Assistant PR Baseline

on:
  push:
    branches:
      - feature-keyword-assistant
    paths:
      - .github/workflows/keyword-assistant-pr-baseline.yml

permissions:
  contents: read

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature-keyword-assistant
      - run: echo "feature head ready for PR"
